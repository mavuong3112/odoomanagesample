<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="menu_polling" model="ir.ui.menu">
        <field name="name">Polling</field>
        <field name="sequence" eval="10"/>
    </record>

    <record id="menu_polling_question" model="ir.ui.menu">
        <field name="name">Polling Question</field>
        <field name="parent_id" ref="menu_polling"/>
        <field name="action" ref="polling.action_poll_question"/>
        <field name="sequence" eval="20"/>
    </record>
</odoo>
